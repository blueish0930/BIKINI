# BIKINI Docs

| File | Description |
|------|-------------|
| [changelog.html](changelog.html) | Custom Geometry Nodes & UX release notes (Blender docs style, **EN / 中文**) |

Open `changelog.html` in a browser. Language is auto-detected from the browser (`zh*` → 中文, otherwise English), can be switched in the header, and is remembered in `localStorage`. Documentation only — does not affect `blender.exe`.
